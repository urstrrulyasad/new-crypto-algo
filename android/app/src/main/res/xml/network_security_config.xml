<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Allow HTTP to AWS EC2 / local backends during rollout. Prefer HTTPS later. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
