name: Deploy Strategy Engine

on:
  push:
    branches:
      - master
    paths:
      - "strategy-engine/**"
      - "docker-compose.gcp.yml"
      - ".github/workflows/strategy-engine-deploy.yml"

  workflow_dispatch:

concurrency:
  group: deploy-vm
  cancel-in-progress: false

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Compute image details
        id: image
        shell: bash
        run: |
          OWNER=$(echo "${{ github.repository_owner }}" | tr '[:upper:]' '[:lower:]')
          IMAGE="ghcr.io/$OWNER/cryptoalgo-strategy-engine"

          echo "owner=$OWNER" >> "$GITHUB_OUTPUT"
          echo "image=$IMAGE" >> "$GITHUB_OUTPUT"

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GHCR_PAT }}

      - name: Build and Push Strategy Engine Image
        uses: docker/build-push-action@v6
        with:
          context: ./strategy-engine
          file: ./strategy-engine/Dockerfile
          push: true
          tags: |
            ${{ steps.image.outputs.image }}:latest
            ${{ steps.image.outputs.image }}:${{ github.sha }}

      - name: Deploy Strategy Engine to EC2
        uses: appleboy/ssh-action@v1.2.2
        env:
          GHCR_PAT: ${{ secrets.GHCR_PAT }}
          GHCR_USER: ${{ github.actor }}
          GHCR_OWNER: ${{ steps.image.outputs.owner }}
          IMAGE_TAG: ${{ github.sha }}

        with:
          host: ${{ secrets.VM_HOST }}
          username: ${{ secrets.VM_USER }}
          key: ${{ secrets.VM_SSH_KEY }}
          port: 22
          timeout: 30s
          command_timeout: 15m
          envs: GHCR_PAT,GHCR_USER,GHCR_OWNER,IMAGE_TAG

          script: |
            set -e

            DEPLOY_DIR="/home/ubuntu/crypto-algo"

            mkdir -p "$DEPLOY_DIR"
            cd "$DEPLOY_DIR"

            echo "$GHCR_PAT" | docker login ghcr.io \
              -u "$GHCR_USER" \
              --password-stdin

            upsert() {
              key="$1"
              value="$2"
              file=".env.gcp"

              touch "$file"

              if grep -q "^${key}=" "$file"; then
                sed -i "s|^${key}=.*|${key}=${value}|" "$file"
              else
                echo "${key}=${value}" >> "$file"
              fi
            }

            upsert "GHCR_OWNER" "$GHCR_OWNER"
            upsert "STRATEGY_ENGINE_TAG" "$IMAGE_TAG"

            IMAGE="ghcr.io/$GHCR_OWNER/cryptoalgo-strategy-engine:$IMAGE_TAG"

            docker pull "$IMAGE"

            docker compose \
              -f docker-compose.gcp.yml \
              --env-file .env.gcp \
              up -d \
              --no-build \
              --no-deps \
              strategy-engine

            docker compose \
              -f docker-compose.gcp.yml \
              --env-file .env.gcp \
              ps strategy-engine

            docker image prune -f
            docker logout ghcr.io || true
